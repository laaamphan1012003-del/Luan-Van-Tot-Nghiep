# Environment Variables for Docker Compose
# Copy this file to .env and modify the values as needed

# MySQL Configuration
MYSQL_ROOT_PASSWORD=L@m0981985353
MYSQL_DATABASE=ocpp_csms
MYSQL_USER=ocpp_user
MYSQL_PASSWORD=ocpp_pass

# Note: The application will use these environment variables when running in Docker
# For local development without Docker, edit database.js directly
